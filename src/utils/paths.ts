// import { dashboardRoutes, pathMeta } from '@/common';

// export const navItems = Object.entries(dashboardRoutes).map(([key, path]) => ({
//   path,
//   title: pathMeta[key as keyof typeof pathMeta].title,
//   Icon: pathMeta[key as keyof typeof pathMeta].icon,
// }));

// export const getTitleFromPath = (pathname: string): string | undefined => {
//   const matchedEntry = Object.entries(dashboardRoutes).find(([, path]) => path === pathname);
//   return matchedEntry ? pathMeta[matchedEntry[0] as keyof typeof dashboardRoutes].title : undefined;
// };
